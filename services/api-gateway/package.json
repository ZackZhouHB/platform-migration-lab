{
  "name": "api-gateway",
  "version": "1.0.0",
  "description": "API Gateway service — routes requests to backend services",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node tests/index.test.js",
    "lint": "echo 'Linting api-gateway...' && exit 0",
    "build": "echo 'Building api-gateway...' && mkdir -p dist && cp -r src/* dist/"
  },
  "dependencies": {
    "express": "^4.18.0"
  }
}
